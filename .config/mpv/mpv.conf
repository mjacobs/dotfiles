# volume=30
osd-msg3=${osd-sym-cc} ${playback-time} ${?duration:/ ${duration}} ${?percent-pos:(${percent-pos}%)} \t [${?file-size:${file-size}}]
osd-level=3

vo=gpu
# Avoid Vulkan video decode on NVIDIA/Wayland; it can wedge during swapchain resize.
hwdec=nvdec-copy
cuda-decode-device=1
